// Central place to point the frontend at the FastAPI backend.
// Change this if you deploy the API somewhere other than localhost:8000.
const APP_CONFIG = {
  API_BASE_URL: "http://localhost:8000/api",
};
